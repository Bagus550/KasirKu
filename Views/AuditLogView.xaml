<UserControl x:Class="KasirKu.Views.AuditLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converters="clr-namespace:KasirKu.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="850">

    <UserControl.Resources>
        <converters:BoolToStatusConverter x:Key="BoolToStatusConverter"/>
    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Filter Tanggal & Jenis Aksi -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Filter Tanggal:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
            <DatePicker SelectedDate="{Binding TanggalAwal, Mode=TwoWay}" Width="120" Margin="0,0,10,0"/>
            <TextBlock Text="s/d" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <DatePicker SelectedDate="{Binding TanggalAkhir, Mode=TwoWay}" Width="120" Margin="0,0,15,0"/>

            <!-- Filter Khusus Aksi Sensitif -->
            <TextBlock Text="Filter Aksi:" VerticalAlignment="Center" FontWeight="Bold" Margin="10,0,10,0"/>
            <ComboBox ItemsSource="{Binding DaftarFilterAksi}" 
                      SelectedItem="{Binding SelectedJenisAksi, Mode=TwoWay}" 
                      Width="160" Margin="0,0,15,0"/>

            <Button Content="🔍 Tampilkan Log" Command="{Binding MuatLogCommand}" Padding="10,5" Background="#0D6EFD" Foreground="White" FontWeight="Bold"/>
        </StackPanel>

        <!-- Tab Content -->
        <TabControl Grid.Row="1">
            <!-- TAB 1: Sesi & Aktivitas Kasir -->
            <TabItem Header="👤 Sesi &amp; Aktivitas Kasir">
                <DataGrid ItemsSource="{Binding DaftarSesiKasir}" AutoGenerateColumns="False" IsReadOnly="True" Margin="10" GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nama Kasir" Binding="{Binding Kasir.Nama, TargetNullValue='-'}" Width="150" FontWeight="SemiBold"/>
                        <DataGridTextColumn Header="Shift Work" Binding="{Binding Shift.NamaShift, TargetNullValue='Admin (Non-Shift)'}" Width="140"/>
                        <DataGridTextColumn Header="Waktu Login" Binding="{Binding WaktuLogin, StringFormat=\{0:dd/MM/yyyy HH:mm\}}" Width="130"/>
                        <DataGridTextColumn Header="Waktu Logout" Binding="{Binding WaktuLogout, StringFormat=\{0:dd/MM/yyyy HH:mm\}, TargetNullValue='🟢 Masih Aktif'}" Width="130"/>
                        <DataGridTextColumn Header="Modal Awal" Binding="{Binding ModalAwal, StringFormat=Rp {0:N0}}" Width="120"/>
                        <DataGridTextColumn Header="Total Omzet" Binding="{Binding TotalTunaiSistem, StringFormat=Rp {0:N0}}" Width="120"/>
                        <DataGridTextColumn Header="Status Sesi" Binding="{Binding IsClosed, Converter={StaticResource BoolToStatusConverter}}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <!-- TAB 2: Audit Trail Aktivitas Sistem & Aksi Sensitif -->
            <TabItem Header="🕒 Log Aktivitas Sistem">
                <DataGrid ItemsSource="{Binding DaftarAuditLog}" AutoGenerateColumns="False" IsReadOnly="True" Margin="10" GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Waktu" Binding="{Binding Waktu, StringFormat=\{0:dd/MM/yyyy HH:mm:ss\}}" Width="140"/>
                        <DataGridTextColumn Header="Pengguna / Kasir" Binding="{Binding Kasir.Nama, TargetNullValue='System'}" Width="150"/>

                        <!-- Kolom Aksi dengan Highlight Warna Sensitivitas -->
                        <DataGridTemplateColumn Header="Aksi" Width="160">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding JenisAksi}" FontWeight="Bold" VerticalAlignment="Center" Margin="5,0">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#0D6EFD"/>
                                                <!-- Default Biru -->
                                                <Style.Triggers>
                                                    <!-- Merah untuk Aksi Berbahaya / Hapus -->
                                                    <DataTrigger Binding="{Binding JenisAksi}" Value="HAPUS_PRODUK">
                                                        <Setter Property="Foreground" Value="#DC3545"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding JenisAksi}" Value="BATAL_TRANSAKSI">
                                                        <Setter Property="Foreground" Value="#DC3545"/>
                                                    </DataTrigger>
                                                    <!-- Oranye untuk Edit / Perubahan Data -->
                                                    <DataTrigger Binding="{Binding JenisAksi}" Value="EDIT_PRODUK">
                                                        <Setter Property="Foreground" Value="#FD7E14"/>
                                                    </DataTrigger>
                                                    <!-- Hijau untuk Tambah / Sukses -->
                                                    <DataTrigger Binding="{Binding JenisAksi}" Value="TAMBAH_PRODUK">
                                                        <Setter Property="Foreground" Value="#198754"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding JenisAksi}" Value="PROSES_TRANSAKSI">
                                                        <Setter Property="Foreground" Value="#198754"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Kolom Keterangan dengan Wrap Text -->
                        <DataGridTextColumn Header="Keterangan Audit" Binding="{Binding Keterangan}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextWrapping" Value="Wrap"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>