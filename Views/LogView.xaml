<UserControl x:Class="KasirKu.Views.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="System Error &amp; Activity Log Viewer" FontSize="20" FontWeight="Bold"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Refresh" Command="{Binding MuatDaftarLogCommand}" Width="90" Height="30" Margin="0,0,10,0"/>
                <Button Content="Export Log File" Command="{Binding ExportLogCommand}" Width="120" Height="30" Background="#007ACC" Foreground="White"/>
            </StackPanel>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Panel Kiri: Daftar File Log -->
            <GroupBox Grid.Column="0" Header="Daftar Log File">
                <ListBox ItemsSource="{Binding DaftarLogFile}"
                         SelectedItem="{Binding SelectedLogFile}"
                         DisplayMemberPath="Name" 
                         Margin="5"/>
            </GroupBox>

            <!-- Panel Kanan: Viewer Isi Log -->
            <GroupBox Grid.Column="2" Header="Detail Log">
                <TextBox Text="{Binding IsiLogContent}"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         FontFamily="Consolas"
                         FontSize="12"
                         Background="#1E1E1E"
                         Foreground="#DCDCDC"
                         Padding="10"/>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>